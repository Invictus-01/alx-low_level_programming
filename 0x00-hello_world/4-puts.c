#include <stdio.h>
/**
 * main - Entry point
 * initialise a string - adding the values
 * print the string using puts
 * Return: 0 once successful
 */
int main(void)
{
	char str1[] = "\"Programming is like building a multilingual puzzle";

	puts(str1);

	return (0);
}
