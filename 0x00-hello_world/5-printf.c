#include <stdio.h>
/**
 * main - Entry point
 * display output to stdout using printf
 * Return: 0 once successful
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
