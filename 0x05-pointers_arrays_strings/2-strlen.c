#include "main.h"

/**
 * _strlen - returns the length of the given string
 * @s: char type pointer
 * Return: length of string
 */

int _strlen(char *s)
{
	int a;

	a = 0;
	for (; s[a] != '\0'; a++)

	return (a);
}
